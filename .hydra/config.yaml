seed: 42
n_samples: 5000
batch_size: ${batch_size}
lr: ${lr}
epochs: ${epochs}
hidden_dim: ${hidden_dim}
val_split: 0.2
noise_std: 0.1
out_dir: models
metrics_path: reports/metrics.json
model_path: models/model.pt
profile: false
profile_path: reports/profile.pstats
use_wandb: true
wandb_project: MLOps_group35-src_mlops_group35
wandb_mode: online
wandb_run_name: null
