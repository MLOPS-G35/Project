
Run data.py



Test:
uv run pytest
uv run pytest -s

uv add --dev coverage
uv run coverage run -m pytest tests
uv run coverage report -m


For linting fix:
uv run ruff check --fix .
uv run ruff format .

docker:
docker run -d \
  --restart unless-stopped \
  -p 8000:8000 \
  ghcr.io/emilianocarrara/mlops_group35-api
